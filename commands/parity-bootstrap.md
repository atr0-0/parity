---
description: Derive a reference web app's system design into machine-readable project docs — archetypes, module catalog, tokens, page inventory, scope ledger. Run once per project before cloning any page. Safe to re-run; gates on review before overwriting reviewed content.
argument-hint: [reference-url]
---

You are running **/parity-bootstrap** — entry point 1 of two.

Goal: replace the cost of every future session re-deriving this app from scratch
with one reviewed set of artifacts they can read instead.

`$ARGUMENTS` may contain the reference app's base URL. Use it if present; otherwise
ask.

## Non-negotiable boundaries

1. **Never write outside `.parity/`** in the target project. Generated scaffolding
   (build scripts, task runners, graders, Dockerfiles, and any pre-existing
   `AGENTS.md` / `CLAUDE.md` / `docs/`) is **read-only background**. It may be
   regenerated by whatever produced it, so edits there are both out of scope and
   liable to be discarded. Read it to learn the project's rules; do not modify it.
2. **Browser automation points at the reference app only** — never at the project's
   own running app.
3. **Facts live in the project, never in this plugin.** If you learn something about
   the reference app, it belongs in `.parity/`.
4. **Structure, layout, and behavior only.** Record how the reference is *built* —
   layout, typography, module composition, interaction. Do not copy its article text,
   headlines, or editorial copy into artifacts or seed data; that content gets
   originally authored later.

## Step 0 — read current state

Read `.parity/SYSTEM_DESIGN.md` if it exists and check its `status`:

- **`reviewed`** → artifacts already exist. Ask whether to **refresh** (re-derive and
  show a diff) or **stop**. Never silently overwrite reviewed content.
- **`draft` / absent** → proceed.

If `module-catalog.json` and `page-inventory.json` exist from earlier work, read them —
on a re-run they carry evidence the first pass did not have (Step 3).

Also read any existing project rules (`AGENTS.md`, `CLAUDE.md`, `README.md`,
`docs/`). You are layering on top of them, so you must know what they already
mandate — especially anything about accessibility, performance, or data handling that
will constrain how pages get built.

## Step 1 — project context and stack profile

The stack is **declared, not guessed**. Read the project's own config files —
package manifests, dependency/lock files, container definitions, app config, and
whatever lives in a scripts directory — and record:

- Framework and language; styling system; the project's blessed data-fetching pattern
- Directory conventions (where components, services, and routes live)
- The commands to install, run a dev server, and verify (type-check / lint / test)
- The dev server's URL and port — the clone-side extractor needs it

Also record the reference app's base URL and any **locale/edition scope** (many sites
serve materially different pages per region; pick one and say so explicitly, because a
capture from the wrong edition looks correct and is wrong).

If a required fact isn't discoverable, **ask** — do not infer a stack from
source-file heuristics.

## Step 2 — recon the page taxonomy

Using the browser, enumerate every reachable page: primary nav, every sub-nav under
it, and the footer. For each, record URL, route, its parent, and its sub-nav children.

State the limit honestly in the artifact: a crawl finds **linked** pages only. Note
anything you know exists but couldn't reach.

## Step 3 — derive archetypes

Sample 2–3 pages per candidate archetype and cluster them. Use the strongest signal
available and **record which one you used**, because they are not equally
trustworthy:

| Rung | Signal | Confidence |
|---|---|---|
| 1 | **Class-family fingerprint** — build-generated class names of the form `Family_element__hash`. Two pages sharing a family *are* the same component in the reference's source | `high` |
| 2 | **Structural fingerprint** — normalized DOM subtree shape plus a computed-style signature | `medium` |
| 3 | **Geometric signature** — grid dimensions, column widths, breakpoints | `low` |
| 4 | **Human labeling** — ask at the review gate | `low` |

Rung 1 is evidence, not inference — prefer it whenever the app exposes it. Many apps
don't (utility-class frameworks, or hashes that change per build); fall down the
ladder and say so.

**The clustering is the heuristic part.** Whether "category page" is its own archetype
or a variant of "hub" is a judgment call, as is variant-vs-separate-component. Record
the judgment and the evidence that supports it, so a later session can disagree with
it on the merits.

Cite concrete evidence for every archetype. "These three pages share class family X
and an identical two-column grid" is evidence; "these look similar" is not.

Record `members` — every route assigned to the cluster, sampled or not. This is the
leverage signal Step 7.5 reads: an archetype with one member describes nothing shared,
however confident the fingerprint.

### On a re-run, read what the build learned

If `module-catalog.json` already exists, **use its occurrence data as an additional
clustering signal.** Routes that ended up sharing extracted components are evidence of a
cluster the reference's own class names did not reveal — and that is precisely the case
where the fingerprint ladder fell to a low rung.

This is what makes a `per-page` verdict reversible. Pages built per-page still populate
the catalog, and a module recurring across several routes is a fact about the site that
the first bootstrap could not see. Without reading it, a re-run samples the same pages
with the same signals and reaches the same verdict — so the recovery path that
`/parity-page` P6 points at would not exist.

Say when a cluster came from catalog evidence rather than from the reference's markup, and
cite the modules and routes. It is a weaker signal than a shared class family, and a
reader should be able to tell the difference.

## Step 4 — seed the module catalog

Per archetype, enumerate the distinct modules and their variants, keyed by
fingerprint. Every entry starts `status: not-built` with `component: null`.

This artifact is the one that prevents accidental duplication later. A written
reuse rule without a catalog of what exists loses to expedience every time — the
catalog is what makes "does this already exist?" answerable in seconds.

## Step 5 — extract design tokens

Colors, type scale, font stacks, spacing steps, breakpoints, border and radius
values.

Record a **font role map**: the reference's faces are often proprietary and get
substituted in the clone, so fidelity comparison is by *role* (display vs. UI), never
by literal font-family string. Getting this wrong makes every module fail forever.

## Step 6 — ask about scope, once

Ask the user: *"Any features you already know are out of scope, deferred, or to be
stubbed?"*

Create `scope-ledger.json` either way — **empty is fine and expected.** It fills in as
work progresses. For each entry captured now, record detection signals (URL patterns,
selectors, class families, link targets) and the handling (omit / render-disabled /
non-interactive / placeholder-target).

The point of the detection signals: when that feature turns up on a *different* page
months later, it gets recognized and handled consistently instead of being quietly
built.

## Step 7 — predict-and-test

Before anyone builds on this taxonomy, measure it.

1. Pick pages the clustering claims belong to an archetype but that were **never
   sampled** (`unsampled: true` in the inventory).
2. Capture each one.
3. Check whether it actually matches that archetype's template.
4. Record `matched / total` in the archetype template and in `SYSTEM_DESIGN.md`.

Report the number plainly, including when it's bad. A taxonomy with a measured
accuracy is a finding; one without is a claim.

If accuracy is poor, **go back to Step 3 and re-cluster** — a wrong taxonomy is far more
expensive after pages are built on it. This is the one loop in the bootstrap. Only when
the number is acceptable, or when re-clustering has been tried and the site genuinely has
no shared structure, does it proceed to the verdict below.

## Step 7.5 — routing verdict

Decide, per archetype, whether it is worth building against. Two tests, both required:

| Test | Default | Rationale |
|---|---|---|
| `members >= minMembers` | 2 | One page is not a pattern. A single-member template invites delta-specs against a shape nothing else shares |
| `predictionAccuracy >= minAccuracy` | 0.7 | Below this the template mispredicts often enough that correcting it costs more than writing the page from its own capture |

Write the result into each `templates/<archetype>.json`:

```jsonc
"routing": {
  "mode":   "template",        // or "per-page"
  "reason": "5 members, predicted 12/14 (0.86)",
  "thresholds": { "minMembers": 2, "minAccuracy": 0.7 }
}
```

`reason` is not decoration — it is what lets a later session disagree with the verdict on
the merits instead of re-deriving it. Cache the same value per route as `mode` in
`page-inventory.json`; a route with no archetype is always `per-page`.

Thresholds live in the file so a project can tune them. Change them deliberately and say
so in the summary; silently loosening them to make an archetype qualify defeats the
measurement.

**`per-page` is a legitimate outcome, not a failure.** It means each page is built from
its own capture, which is the same pipeline minus the template layer — same capture, same
diff, same gates. On a site where every page genuinely differs, that is the cheaper route
and the template layer would be pure overhead. Do not soften a verdict to avoid reporting
it.

## Step 8 — emit artifacts, then gate

Write every artifact to `.parity/` per the schemas in
`${CLAUDE_PLUGIN_ROOT}/templates/ARTIFACTS.md`:

```
.parity/
├── SYSTEM_DESIGN.md          # the HLD a new session reads instead of re-deriving
├── page-inventory.json
├── module-catalog.json
├── tokens.json
├── scope-ledger.json         # may be empty
├── deviations.json           # may be empty
├── WORKFLOW.md               # the page pipeline, with this project's real paths
└── templates/<archetype>.json
```

Emit a template file even for an archetype that failed Step 7.5 — the evidence and the
member list stay useful — but stamped `routing.mode: "per-page"` so `/parity-page` never
delta-specs against it. A disqualified archetype is a record, not an instruction.

All are written with `status: draft`. Then **present a summary and stop**:

- Archetypes found, with confidence and evidence
- Prediction accuracy, and the routing verdict per archetype with its reason
- **How many routes take each mode** — the one line that tells the operator what the
  build will actually cost
- Module count and page count
- Anything you could not determine, and any rung-3/4 guesses that need a human call

The user promotes artifacts to `status: reviewed`. Nothing downstream treats them as
authoritative until then — `/parity-page` refuses to build against `draft`.

## Reporting rules

- **Never present a low-confidence cluster as settled.** Mark it, cite the rung, and
  flag it for review.
- **Never silently drop a module you couldn't classify.** Unmatched modules go in
  `unmatched` — they're the signal the taxonomy is incomplete.
- **Report bad numbers.** A poor prediction accuracy discovered now is cheap; the same
  fact discovered after ten pages is not.
- **Say plainly when nothing qualified.** If no archetype passes Step 7.5, report that
  every page will be built per-page and why. That is a real finding about the site — some
  sites are genuinely bespoke per page — and it saves the operator paying for a template
  layer that would predict nothing. Never present it as a failure of the bootstrap.
